# 🚨 window.alert()

---

## 🇺🇸 English Version

### 🎯 Purpose
The `window.alert()` method is used to display a modal dialog box with a specified message and an **OK** button.
*   **Behavior:** It pauses the script execution until the user clicks "OK".
*   **Return:** It returns `undefined`.

### 🏗️ Context of Use
It is rarely used in modern production websites because it blocks the user interface and looks outdated. However, it is useful in specific contexts:
1.  **Debugging (Beginner):** To quickly check if a code block is being executed or to see a variable's value.
2.  **Critical Warnings:** When you need to ensure the user sees a message before proceeding (e.g., "System is going down for maintenance").
3.  **Educational Projects:** Perfect for learning logic without worrying about HTML/CSS UI.

### 💻 Syntax
```javascript
window.alert("Hello, World!");
// Or simply:
alert("Message here");
```

---

## 🇧🇷 Versão em Português

### 🎯 Propósito
O método `window.alert()` é usado para exibir uma caixa de diálogo modal com uma mensagem específica e um botão **OK**.
*   **Comportamento:** Ele pausa a execução do script até que o usuário clique em "OK".
*   **Retorno:** Retorna `undefined`.

### 🏗️ Contexto de Uso
É raramente usado em sites modernos de produção porque bloqueia a interface do usuário e tem uma aparência antiga. No entanto, é útil em contextos específicos:
1.  **Depuração (Iniciante):** Para verificar rapidamente se um bloco de código está sendo executado ou ver o valor de uma variável.
2.  **Avisos Críticos:** Quando você precisa garantir que o usuário veja uma mensagem antes de prosseguir (ex: "O sistema entrará em manutenção").
3.  **Projetos Educacionais:** Perfeito para aprender lógica sem se preocupar com a interface HTML/CSS.

### 💻 Sintaxe
```javascript
window.alert("Olá, Mundo!");
// Ou simplesmente:
alert("Mensagem aqui");
```

---

### 🙌 Credits / Créditos
Inspired by **Curso em Vídeo (Gustavo Guanabara)**.
