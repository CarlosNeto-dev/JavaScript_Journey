/**
 * Displays a welcome message to the user using browser alert.
 *
 * @function showMessageAlert
 *
 * @param {string} message The message will be put.
 *
 * @returns {void}
 *
 */

export function showMessageAlert(message) {
    // Exibe uma mensagem ao entrar no site.

    window.alert(message);
}
