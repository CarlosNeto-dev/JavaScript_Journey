# 📝 window.prompt()

---

## 🇺🇸 English Version

### 🎯 Purpose
The `window.prompt()` method is used to collect input from the user. It displays a dialog box with a message, a text input field, an **OK** button, and a **Cancel** button.

### ⚙️ How it Works
1.  **Blocking Behavior:** Pauses execution waiting for the user to type something and click a button.
2.  **Return Value (Crucial Detail):**
    *   If the user clicks **OK**, it returns the text entered as a **String**.
    *   If the user clicks **Cancel**, it returns `null`.
    *   ⚠️ **Important:** Even if the user types a number (e.g., `10`), it returns `"10"` (a string). You must convert it if you want to do math!

### 🏗️ Context of Use
*   **Simple Data Collection:** Asking for a name, age, or simple value in educational scripts.
*   **Quick Testing:** Testing functions that require arguments without hardcoding them.
*   **Legacy Systems:** Older internal tools might still use this for quick inputs.

### 💻 Syntax
```javascript
// prompt(message, defaultValue);
let name = window.prompt("What is your name?", "Guest");

if (name !== null) {
    console.log("Hello, " + name + "!");
} else {
    console.log("User cancelled.");
}
```

### 👨‍🔬 Practice Time! (Your Turn)
I have created a specific file for this command.
**You are the participant!** Open the example file. Try changing the default value (the second parameter). Also, try typing a number and checking its type with `typeof` in the console.

👉 **Go to the Lab:** [window-prompt.js](../examples/window-prompt.js)

### 📚 Official Documentation
*   [MDN: window.prompt()](https://developer.mozilla.org/en-US/docs/Web/API/Window/prompt)

### 🐛 Found a Bug?
If you find any error, please **open an Issue** in this repository!

---

### 🙌 Credits
*   **Inspiration:** **Curso em Vídeo** by **Gustavo Guanabara**. 🎓
*   **Content:** Textual content generated by AI for completeness.
*   **Structure & Ideas:** Planned and organized by **Neto**.

---

## 🇧🇷 Versão em Português

### 🎯 Propósito
O método `window.prompt()` é usado para coletar dados do usuário. Ele exibe uma caixa de diálogo com uma mensagem, um campo de texto, um botão **OK** e um botão **Cancelar**.

### ⚙️ Como Funciona
1.  **Comportamento Bloqueante:** Pausa a execução aguardando o usuário digitar algo e clicar em um botão.
2.  **Valor de Retorno (Detalhe Crucial):**
    *   Se o usuário clicar em **OK**, ele retorna o texto digitado como uma **String** (texto).
    *   Se o usuário clicar em **Cancelar**, ele retorna `null`.
    *   ⚠️ **Importante:** Mesmo que o usuário digite um número (ex: `10`), ele retorna `"10"` (uma string). Você deve convertê-lo se quiser fazer contas!

### 🏗️ Contexto de Uso
*   **Coleta de Dados Simples:** Perguntar nome, idade ou valor simples em scripts educacionais.
*   **Testes Rápidos:** Testar funções que requerem argumentos sem precisar escrevê-los fixos no código.
*   **Sistemas Legados:** Ferramentas internas antigas ainda podem usar isso para entradas rápidas.

### 💻 Sintaxe
```javascript
// prompt(mensagem, valorPadrao);
let nome = window.prompt("Qual é o seu nome?", "Visitante");

if (nome !== null) {
    console.log("Olá, " + nome + "!");
} else {
    console.log("Usuário cancelou.");
}
```

### 👨‍🔬 Hora de Praticar! (Sua Vez)
Eu criei um arquivo específico para este comando.
**Você é o participante!** Abra o arquivo de exemplo. Tente mudar o valor padrão (o segundo parâmetro). Tente também digitar um número e verificar o tipo dele com `typeof` no console.

👉 **Vá para o Laboratório:** [window-prompt.js](../examples/window-prompt.js)

### 📚 Documentação Oficial
*   [MDN: window.prompt()](https://developer.mozilla.org/pt-BR/docs/Web/API/Window/prompt)

### 🐛 Encontrou um Bug?
Se você encontrar qualquer erro, por favor **abra uma Issue** neste repositório!

---

### 🙌 Créditos
*   **Inspiração:** **Curso em Vídeo** do professor **Gustavo Guanabara**. 🎓
*   **Conteúdo:** Conteúdo textual gerado por IA para completude.
*   **Estrutura e Ideias:** Planejado e organizado por **Neto**.
