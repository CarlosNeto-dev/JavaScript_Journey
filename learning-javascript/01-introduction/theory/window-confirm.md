# ❓ window.confirm()

---

## 🇺🇸 English Version

### 🎯 Purpose
The `window.confirm()` method is used when you need the user to make a binary decision (Yes or No). It displays a modal dialog with a message, an **OK** button, and a **Cancel** button.

### ⚙️ How it Works
1.  **Blocking Behavior:** Like `alert`, it pauses the script execution until the user clicks a button.
2.  **Return Value (The Most Important Part):** It returns a **Boolean** value.
    *   Returns `true` if the user clicks **OK**.
    *   Returns `false` if the user clicks **Cancel**.

### 🏗️ Context of Use
*   **Critical Actions:** "Are you sure you want to delete this file?"
*   **Navigation:** "You have unsaved changes. Do you really want to leave?"
*   **Simple Logic:** Branching your code execution based on the user's choice (If `true` do A, if `false` do B).

### 💻 Syntax
```javascript
let userAgreed = window.confirm("Do you accept the terms?");

if (userAgreed) {
    console.log("User said YES!");
} else {
    console.log("User said NO.");
}
```

### 👨‍🔬 Practice Time! (Your Turn)
I have created a specific file for this command.
**You are the participant!** Open the example file, change the message inside the `confirm()`, and try to create different reactions for "OK" and "Cancel".

👉 **Go to the Lab:** [window-confirm.js](../examples/window-confirm.js)

### 📚 Official Documentation
*   [MDN: window.confirm()](https://developer.mozilla.org/en-US/docs/Web/API/Window/confirm)

### 🐛 Found a Bug?
If you find any error, please **open an Issue** in this repository!

---

### 🙌 Credits
*   **Inspiration:** **Curso em Vídeo** by **Gustavo Guanabara**. 🎓
*   **Content:** Textual content generated by AI for completeness.
*   **Structure & Ideas:** Planned and organized by **Neto**.

---

## 🇧🇷 Versão em Português

### 🎯 Propósito
O método `window.confirm()` é usado quando você precisa que o usuário tome uma decisão binária (Sim ou Não). Ele exibe uma caixa de diálogo modal com uma mensagem, um botão **OK** e um botão **Cancelar**.

### ⚙️ Como Funciona
1.  **Comportamento Bloqueante:** Assim como o `alert`, ele pausa a execução do script até que o usuário clique em um botão.
2.  **Valor de Retorno (A Parte Mais Importante):** Ele retorna um valor **Booleano**.
    *   Retorna `true` (verdadeiro) se o usuário clicar em **OK**.
    *   Retorna `false` (falso) se o usuário clicar em **Cancelar**.

### 🏗️ Contexto de Uso
*   **Ações Críticas:** "Tem certeza que deseja excluir este arquivo?"
*   **Navegação:** "Você tem alterações não salvas. Deseja realmente sair?"
*   **Lógica Simples:** Desviar a execução do seu código com base na escolha do usuário (Se `true` faça A, se `false` faça B).

### 💻 Sintaxe
```javascript
let usuarioConcordou = window.confirm("Você aceita os termos?");

if (usuarioConcordou) {
    console.log("Usuário disse SIM!");
} else {
    console.log("Usuário disse NÃO.");
}
```

### 👨‍🔬 Hora de Praticar! (Sua Vez)
Eu criei um arquivo específico para este comando.
**Você é o participante!** Abra o arquivo de exemplo, mude a mensagem dentro do `confirm()` e tente criar reações diferentes para "OK" e "Cancelar".

👉 **Vá para o Laboratório:** [window-confirm.js](../examples/window-confirm.js)

### 📚 Documentação Oficial
*   [MDN: window.confirm()](https://developer.mozilla.org/pt-BR/docs/Web/API/Window/confirm)

### 🐛 Encontrou um Bug?
Se você encontrar qualquer erro, por favor **abra uma Issue** neste repositório!

---

### 🙌 Créditos
*   **Inspiração:** **Curso em Vídeo** do professor **Gustavo Guanabara**. 🎓
*   **Conteúdo:** Conteúdo textual gerado por IA para completude.
*   **Estrutura e Ideias:** Planejado e organizado por **Neto**.
